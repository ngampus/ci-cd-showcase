{
	"name": "ci-cd-showcase",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"deploy:prod": "wrangler deploy --env production"
	},
	"devDependencies": {
		"wrangler": "^4.0.0",
		"@cloudflare/workers-types": "^5.0.0",
		"typescript": "^5.0.0"
	}
}
